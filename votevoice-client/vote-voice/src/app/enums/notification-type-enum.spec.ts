import { NotificationTypeEnum } from './notification-type-enum';

describe('NotificationTypeEnum', () => {
  it('should create an instance', () => {
    expect(new NotificationTypeEnum()).toBeTruthy();
  });
});
